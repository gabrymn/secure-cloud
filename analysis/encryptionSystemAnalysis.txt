Encryption System Analysis  

pwd = input()
dkey = PKBF2(pwd, 256bit)

recovery_key, iv_recovery_key = random_AESKey(256bit)
cipher_key, iv_cipher_key = random_AESKey(256bit)

aes_encryption (cipher_key) with (recovery_key)
aes_encryption (recovery_key) with (dkey)

store on db:
    pwd_argon2ID_hash
    rkey_argon2ID_hash
    rkey_encrypted
    ckey_encrypted
    iv_rkey
    iv_ckey